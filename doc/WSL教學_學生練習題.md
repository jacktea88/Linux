# WSL 學生練習題

> 共 6 堂課的練習，每堂課結束後獨立完成。  
> 📌 **規則**：請自己嘗試，遇到問題先用 `--help` 或 `man` 查詢，再問老師。

---

## 第 1 堂練習：認識環境

**完成以下任務，並將輸出結果截圖或記錄下來。**

---

**Q1｜基本資訊查詢**

執行指令，找出以下資訊並填入空白：

```bash
# 執行後填入答案
whoami        → 我的使用者名稱是：__________
hostname      → 我的主機名稱是：__________
uname -r      → Linux 核心版本是：__________
echo $SHELL   → 我使用的 Shell 是：__________
```

---

**Q2｜查詢目前位置**

1. 啟動 WSL 後，執行 `pwd`，你目前在哪個目錄？
2. 執行 `ls -la ~`，家目錄下有哪些隱藏檔案？（以 `.` 開頭的）列出 3 個：

```
1. ___________
2. ___________
3. ___________
```

---

**Q3｜使用說明查詢**

執行 `ls --help`，回答：

- `-t` 選項的功能是什麼？  
  答：___________________________________________

- `-S` 選項的功能是什麼？  
  答：___________________________________________

---

**Q4｜挑戰題**

執行一個指令，同時顯示：詳細格式（-l）、人類可讀大小（-h）、依時間排序（-t），  
列出家目錄下的所有檔案（包含隱藏）。

```bash
指令是：_______________________________
```

---

## 第 2 堂練習：目錄與檔案操作

**建立以下目錄結構，並完成各項任務。**

---

**Q1｜建立目錄結構**

在家目錄下建立以下結構（用一行指令完成）：

```
~/practice/
├── week1/
├── week2/
└── week3/
    └── notes/
```

```bash
指令是：_______________________________
```

完成後用 `ls -R ~/practice/` 確認，並截圖。

---

**Q2｜檔案建立與內容寫入**

在 `~/practice/week1/` 目錄下：

1. 建立一個名為 `myinfo.txt` 的檔案
2. 用 `echo` 指令將你的名字寫入（覆蓋）
3. 用 `echo` 指令將今天日期附加到第二行（不覆蓋）
4. 用 `cat` 指令確認內容

```bash
# 寫出你使用的 4 個指令：
1. ___________________________
2. ___________________________
3. ___________________________
4. ___________________________
```

---

**Q3｜複製與移動**

1. 將 `myinfo.txt` 複製到 `~/practice/week2/`，命名為 `myinfo_copy.txt`
2. 將 `~/practice/week2/myinfo_copy.txt` 移動到 `~/practice/week3/notes/`
3. 確認 `week2/` 已空、`week3/notes/` 有檔案

```bash
# 寫出你使用的指令：
複製：___________________________
移動：___________________________
確認：___________________________
```

---

**Q4｜挑戰題**

在 `~/practice/week1/` 下建立 5 個檔案：`a.txt`、`b.txt`、`c.txt`、`d.txt`、`e.txt`，  
然後用**一行指令**刪除其中 `b.txt`、`d.txt`。

```bash
建立5個檔案（一行）：___________________________
刪除2個檔案（一行）：___________________________
```

---

## 第 3 堂練習：文字處理與搜尋

**使用以下資料檔完成練習。**

---

**準備資料（先執行）**

```bash
mkdir -p ~/practice/week3/search && cd ~/practice/week3/search

cat > employees.txt << 'EOF'
Alice Engineer Taipei 85000
Bob Manager Kaohsiung 120000
Carol Engineer Tainan 88000
David Designer Taipei 75000
Eve Engineer Taipei 92000
Frank Manager Taipei 130000
Grace Designer Kaohsiung 78000
EOF
```

---

**Q1｜grep 搜尋**

用 `grep` 回答以下問題：

1. 列出所有「Engineer」員工：
```bash
指令：___________________________
```

2. 列出所有在「Taipei」的員工（顯示行號）：
```bash
指令：___________________________
```

3. 列出**不是** Engineer 的員工：
```bash
指令：___________________________
```

4. 檔案中共有幾個 Manager？
```bash
指令：___________________________
答：___ 個
```

---

**Q2｜find 搜尋**

1. 找出 `~/practice/` 下所有 `.txt` 檔案：
```bash
指令：___________________________
```

2. 找出 `~/practice/` 下所有目錄（type d）：
```bash
指令：___________________________
```

---

**Q3｜管線（pipe）組合**

1. 列出 `/usr/bin/` 下所有含 `zip` 的指令：
```bash
指令：___________________________
```

2. 統計 `employees.txt` 共有幾行：
```bash
指令：___________________________
```

3. 將 Taipei 的員工資料存到新檔案 `taipei_staff.txt`：
```bash
指令：___________________________
```

---

**Q4｜挑戰題**

只用一行指令（可用管線），找出 `employees.txt` 中：  
職稱是 Engineer 且城市是 Taipei 的員工，並按字母順序排列。

```bash
指令：___________________________
結果（員工名字）：___________________________
```

---

## 第 4 堂練習：權限管理

---

**Q1｜讀懂權限**

執行 `ls -l /etc/passwd`，輸出如下：
```
-rw-r--r-- 1 root root 2847 Mar 24 09:00 /etc/passwd
```

回答：
- 擁有者（owner）的權限是：___ （數字）= ___ （rwx）
- 群組（group）的權限是：___ （數字）= ___ （rwx）
- 其他人（others）的權限是：___ （數字）= ___ （rwx）
- 這個檔案能被一般使用者執行嗎？答：___

---

**Q2｜chmod 練習**

在 `~/practice/week1/` 建立 `test.sh`，內容如下：

```bash
echo '#!/bin/bash' > ~/practice/week1/test.sh
echo 'echo "我會執行了！"' >> ~/practice/week1/test.sh
```

接著：
1. 嘗試執行 `~/practice/week1/test.sh`（會失敗），記錄錯誤訊息：
```
錯誤訊息：___________________________
```

2. 用 `chmod` 給予執行權限後再執行：
```bash
chmod 指令：___________________________
執行指令：___________________________
```

3. 執行成功後，用 `ls -l` 確認權限，新的權限字串是：
```
___________________________
```

---

**Q3｜sudo 練習**

1. 不用 sudo，嘗試建立 `/opt/test.txt`，記錄錯誤：
```bash
touch /opt/test.txt
錯誤：___________________________
```

2. 改用 sudo 執行，是否成功？
```bash
指令：___________________________
結果：___________________________
```

3. 執行完後，用 sudo 刪除剛才建立的測試檔案：
```bash
指令：___________________________
```

---

**Q4｜挑戰題**

建立一個檔案 `secret.txt`，設定成：
- 擁有者：可讀寫（不可執行）
- 群組：只能讀
- 其他人：無任何權限

```bash
建立指令：___________________________
chmod 指令：___________________________
ls -l 確認輸出：___________________________
```

---

## 第 5 堂練習：套件管理

---

**Q1｜安裝與確認**

安裝以下套件，並確認版本：

| 套件 | 安裝指令 | 版本確認指令 | 你的版本 |
|------|---------|------------|--------|
| `tree` | `sudo apt install tree -y` | `tree --version` | ______ |
| `curl` | `sudo apt install curl -y` | `curl --version` | ______ |
| `git` | `sudo apt install git -y` | `git --version` | ______ |

---

**Q2｜套件查詢**

1. 搜尋名稱含 `python3` 的套件（只取前 5 行）：
```bash
指令：___________________________
```

2. 查看 `git` 套件的詳細資訊（apt show），找出它的「Description」第一行：
```bash
指令：___________________________
描述：___________________________
```

3. 用 `which` 確認 git 安裝在哪個路徑：
```bash
指令：___________________________
路徑：___________________________
```

---

**Q3｜Python 虛擬環境**

在 `~/practice/` 下建立 Python 虛擬環境 `myenv`，並完成：

```bash
# 填入指令
建立 venv：___________________________
啟動 venv：___________________________
# 啟動後提示字元變為：(myenv) user@host:...
安裝 requests 套件：___________________________
確認已安裝（pip list）：___________________________
離開 venv：___________________________
```

---

**Q4｜挑戰題**

在 `~/practice/` 下建立虛擬環境，安裝 `httpx`，  
然後建立 `test_http.py`，內容能成功 `import httpx` 並印出版本號。

```python
# 寫出 test_http.py 的內容：
import ______
print(______.__version__)
```

---

## 第 6 堂練習：整合實戰

**完成一個完整的小型任務流程。**

---

**任務：建立個人備忘錄系統**

要求：在 WSL 中建立一套簡單的備忘錄管理系統。

---

**Step 1｜目錄初始化**

建立以下結構：
```
~/memo_system/
├── notes/
├── archive/
└── scripts/
```
```bash
指令：___________________________
```

---

**Step 2｜建立新增備忘的腳本**

在 `~/memo_system/scripts/` 建立 `add_note.sh`，功能：
- 接受一個參數（備忘內容）
- 將內容加上時間戳記，寫入 `~/memo_system/notes/notes.txt`

```bash
#!/bin/bash
# 補全這個腳本：
echo "$(___________): $1" >> ~/memo_system/notes/notes.txt
echo "備忘已儲存！"
```

```bash
# 給予執行權限的指令：
___________________________
```

---

**Step 3｜測試腳本**

```bash
# 執行 3 次，新增不同備忘：
~/memo_system/scripts/add_note.sh "___________________________"
~/memo_system/scripts/add_note.sh "___________________________"
~/memo_system/scripts/add_note.sh "___________________________"

# 查看備忘錄內容的指令：
___________________________
```

---

**Step 4｜搜尋備忘**

用 `grep` 在 `notes.txt` 中搜尋特定關鍵字，並將結果存到 `archive/search_result.txt`：

```bash
指令：___________________________
```

---

**Step 5｜用 Windows 檔案總管開啟**

用一行指令，用 Windows 檔案總管打開 `~/memo_system/` 目錄：

```bash
指令：___________________________
```

---

**Step 6｜自我評量**

| 項目 | 完成？ |
|------|--------|
| 目錄結構建立正確 | ☐ |
| 腳本可以正常執行 | ☐ |
| notes.txt 有 3 筆以上記錄 | ☐ |
| 搜尋結果正確輸出 | ☐ |
| 能用 Windows 開啟目錄 | ☐ |
